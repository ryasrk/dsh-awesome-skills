node_modules/
lib/
*.tsbuildinfo
# Derived caches: rebuilt from the index on first query. They live next to the
# canonical corpus, not in the package's skills/ dir.
gramcache.json
gramcache.fp.json
qcache.json
